export default interface IRobotDTO {
    domainId: string;
    identification: string;
    nickname: string;
    robotType: string
    serialNumber: string;
    description: string;
    state: boolean;
}
