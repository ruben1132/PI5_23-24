export interface Profile{
    id: string;
    name: string;
    nif: number;
    email: string;
    phone: number;
}

export interface ProfileWithPassword{
    id: string;
    name: string;
    nif: number;
    email: string;
    password: string;
    phone: number;
}

